# AI

当前项目为[spacex-engine-poc]
已经考虑了下面条件：
核心框架: Nuxt + Vue 3
语言: TypeScript
样式方案: Unocss CSS
Auto-imports 机制
无主题模式(以黑暗模式设计)
无响应式布局
严格按我的代码风格写
请用一种清晰的文本描述格式来展示代码变更，明确指出‘从哪一行开始’到‘哪一行结束’的旧代码块，应该被替换为新的代码块
下面回答全部使用中文回答

## with new code

Merlin 1D Vacuum 版图形绘制改变一下，按我的要求来，
中心还是21半径的圆
再来一个70半径的圈，颜色rgba(128,128,128,0.3) stroke-width="3"
然后再来一个中心白色到无色的渐变

### history

将当前的项目优化一下，提取重复的逻辑与组件，以便我再添加一个火箭发动机喷口形状，可以使用同样的配置逻辑。
当前为 梅林1D海平面 版
再添加一个 梅林1D真空版
该发动机仅一个，大小与9个圆的半径相同，但真空版喷口更大，所以在svg上在其外围添加一个很细圈（与中心圆同步亮），表示真空版喷口

### history

将目前的svg中的 9个圆，表示火箭发动机的状态
一共有两种状态，开启与关闭
分别使用
rgba(255,255,255,0.9)
与 rgba(128,128,128,0.3)

然后我希望创建一个序列功能

比如展示放在中间，右上角有序列配置与操作的按钮
点击打开一个dialog
默认状态为全部关闭，序列配置仅为一个
我可以添加下一个节点，告诉哪几个火箭发动机开启或关闭
配置完序列后，需要保存。
在svg的下面有一个 下一步的 按钮，直接全部节点结束